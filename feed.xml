<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
<link href="https://matklad.github.io/feed.xml" rel="self" type="application/atom+xml"/>
<link href="https://matklad.github.io" rel="alternate" type="text/html"/>
<updated>2026-04-27T00:35:17.101Z</updated>
<id>https://matklad.github.io/feed.xml</id>
<title type="html">matklad</title>
<subtitle>Yet another programming blog by Alex Kladov aka matklad.</subtitle>
<author><name>Alex Kladov</name></author>

<entry>
<title type="text">Sub-microsecond xgboost inference</title>
<link href="https://matklad.github.io/2026/03/31/sub-microsecond-inference-xgboost.html" rel="alternate" type="text/html" title="Sub-microsecond xgboost inference" />
<published>2026-03-31T00:00:00+00:00</published>
<updated>2026-03-31T00:00:00+00:00</updated>
<id>https://matklad.github.io/2026/03/31/sub-microsecond-inference-xgboost</id>
<author><name>Alex Kladov</name></author>
<summary type="html"><![CDATA[Experiments in cache locality, data structures, cpu isolation, etc]]></summary>
<content type="html" xml:base="https://matklad.github.io/2026/03/31/sub-microsecond-inference-xgboost.html"><![CDATA[
<h1><span>Sub-microsecond xgboost inference</span> <time class="meta" datetime="2026-03-31">Mar 31, 2026</time></h1>
<p><strong><strong><span>Experiments in cache locality, data structures, cpu isolation, etc</span></strong></strong></p>
<p><span>I stumbled </span><a href="https://www.xelera.io/post/introducing-xelera-silva-cpu-only-sub-microsecond-gbt-inference-on-any-machine"><span>this</span></a><span> and was amazed that sub-microsecond inference was possible.</span></p>
<p><span>And of course, I wanted to see if I could beat it.</span></p>

<figure class="blockquote">
<blockquote><p><span>Disclaimer: code has been generated by Claude Code. The writing here is entirely my own.</span></p>
</blockquote>

</figure>
<section id="Results">

    <h2>
    <a href="#Results"><span>Results</span> </a>
    </h2>
<p><span>A lot of fun! I was able to get</span></p>
<ul>
<li>
<span>7.8% faster p50</span>
</li>
<li>
<span>2.8% faster p99</span>
</li>
<li>
<span>p99 of 798ns</span>
</li>
</ul>
<p><span>A few caveats. This was not an apples to apples comparison</span></p>
<ul>
<li>
<span>different cpus</span>
</li>
<li>
<span>different test data</span>
</li>
<li>
<span>different models</span>
</li>
</ul>
</section>
<section id="Refresher">

    <h2>
    <a href="#Refresher"><span>Refresher</span> </a>
    </h2>
<p><span>During training, you are picking some subset of features and using them to build a decision tree.</span></p>
<p><span>You then repeat this multiple times, yielding multiple trees.</span></p>
<p><span>At inference time, you are running through each tree and doing a weighted sum of the resultant leaf</span>
<span>value, weighted by the learning rate with some initial prediction added to this value.</span></p>
<p><span>Putting some numbers to it, for our small dataset we used 64 features and 200 trees, with a depth of 5.</span>
<span>The depth is referring to the distance from the root node to the deepest leaf node.</span></p>
<p><span>Decision trees look something like this. You have a serieis of conditionals, with other nodes off of them.</span></p>

<figure class="code-block">


<pre><code><span class="line">  income &lt;= 52k</span>
<span class="line">  ├─ yes → credit_score &lt;= 680</span>
<span class="line">  │  ├─ yes → age &lt;= 30</span>
<span class="line">  │  │  ├─ yes → Leaf: -0.20</span>
<span class="line">  │  │  └─ no  → Leaf: +0.35</span>
<span class="line">  │  └─ no  → dependents &gt; 2</span>
<span class="line">  │     ├─ yes → Leaf: -0.50</span>
<span class="line">  │     └─ no  → Leaf: +0.10</span>
<span class="line">  └─ no  → debt_ratio &lt;= 0.45</span>
<span class="line">     ├─ yes → open_accounts &lt;= 3</span>
<span class="line">     │  ├─ yes → Leaf: +0.25</span>
<span class="line">     │  └─ no  → Leaf:  0.00</span>
<span class="line">     └─ no  → utilization &lt;= 0.35</span>
<span class="line">        ├─ yes → Leaf: +0.60</span>
<span class="line">        └─ no  → Leaf: -0.15</span></code></pre>

</figure>

<figure class="code-block">


<pre><code><span class="line">  income &lt;= 46k</span>
<span class="line">  ├─ yes → age &lt;= 28</span>
<span class="line">  │  ├─ yes → credit_score &lt;= 690</span>
<span class="line">  │  │  ├─ yes → Leaf: -0.80</span>
<span class="line">  │  │  └─ no  → Leaf: -0.35</span>
<span class="line">  │  └─ no  → dependents &lt;= 1</span>
<span class="line">  │     ├─ yes → Leaf: +0.12</span>
<span class="line">  │     └─ no  → Leaf: -0.20</span>
<span class="line">  └─ no  → age &lt;= 55</span>
<span class="line">     ├─ yes → credit_score &lt;= 700</span>
<span class="line">     │  ├─ yes → Leaf: +0.28</span>
<span class="line">     │  └─ no  → Leaf: -0.60</span>
<span class="line">     └─ no  → age &lt;= 45</span>
<span class="line">        ├─ yes → Leaf: +0.18</span>
<span class="line">        └─ no  → Leaf: 0.00</span></code></pre>

</figure>
<p><span>So if we have some input training data</span></p>

<figure class="code-block">


<pre><code><span class="line">{</span>
<span class="line">    "income": 50000,</span>
<span class="line">    "credit_score": 700,</span>
<span class="line">    "age": 34,</span>
<span class="line">    "dependents": 2,</span>
<span class="line">    "debt_ratio": 0.40,</span>
<span class="line">    "open_accounts": 4,</span>
<span class="line">    "utilization": 0.22</span>
<span class="line">}</span></code></pre>

</figure>
<p><span>We</span>&rsquo;<span>d walk through each tree to get its leaf node</span></p>

<figure class="code-block">


<pre><code><span class="line">- Tree 1 → +0.10</span>
<span class="line">    - income &lt;= 52000 → yes</span>
<span class="line">    - credit_score &lt;= 680 → no</span>
<span class="line">    - dependents &gt; 2 → no (2 &gt; 2 is false)</span>
<span class="line">    - Leaf: +0.10</span>
<span class="line">- Tree 2 → +0.28</span>
<span class="line">    - income &lt;= 46000 → no</span>
<span class="line">    - age &lt;= 55 → yes</span>
<span class="line">    - credit_score &lt;= 700 → yes (700 &lt;= 700)</span>
<span class="line">    - Leaf: +0.28</span></code></pre>

</figure>
<p><span>And finally aggregate those.</span></p>

<figure class="code-block">


<pre><code><span class="line">Given</span>
<span class="line">initial prediction = 0;</span>
<span class="line">learning rate = 0.1;</span>
<span class="line"></span>
<span class="line">raw score = initial prediction + learning rate * sum(tree leaf values)</span>
<span class="line">raw score = 0 + 0.1 * (0.10 + 0.28)</span>
<span class="line">raw score = 0.038</span></code></pre>

</figure>
</section>
<section id="Naive-Pseudocode">

    <h2>
    <a href="#Naive-Pseudocode"><span>Naive Pseudocode</span> </a>
    </h2>

<figure class="code-block">


<pre><code><span class="line">func tree1(input) {</span>
<span class="line">  if (input.income &lt;= 52000) {</span>
<span class="line">    if (input.credit_score &lt;= 680) {</span>
<span class="line">      if (input.age &lt;= 30) {</span>
<span class="line">        return -0.20</span>
<span class="line">      } else {</span>
<span class="line">        return +0.35</span>
<span class="line">      }</span>
<span class="line">    } else {</span>
<span class="line">      if (input.dependents &gt; 2) {</span>
<span class="line">        return -0.50</span>
<span class="line">      } else {</span>
<span class="line">        return +0.10</span>
<span class="line">      }</span>
<span class="line">    }</span>
<span class="line">  } else {</span>
<span class="line">    if (input.debt_ratio &lt;= 0.45) {</span>
<span class="line">      if (input.open_accounts &lt;= 3) {</span>
<span class="line">        return +0.25</span>
<span class="line">      } else {</span>
<span class="line">        return 0.00</span>
<span class="line">      }</span>
<span class="line">    } else {</span>
<span class="line">      if (input.utilization &lt;= 0.35) {</span>
<span class="line">        return +0.60</span>
<span class="line">      } else {</span>
<span class="line">        return -0.15</span>
<span class="line">      }</span>
<span class="line">    }</span>
<span class="line">  }</span>
<span class="line">}</span>
<span class="line"></span>
<span class="line">// similar for tree2</span>
<span class="line"></span>
<span class="line">func rawScore(input, trees, initial_prediction, training_rate) {</span>
<span class="line">  let sum = 0;</span>
<span class="line">  for tree in trees {</span>
<span class="line">    sum += tree(input)</span>
<span class="line">  }</span>
<span class="line"></span>
<span class="line">  return initial_prediction + training_rate * sum</span>
<span class="line">}</span></code></pre>

</figure>
<p><span>Very simple.</span></p>
<p><span>A few performance things we can see that stick out:</span></p>
<ol>
<li>
<p><span>First thing we can see is that we could calculate each tree concurrently. That is, there is</span>
<span>no path dependency on the tree calculations. Calculating the ith tree does not depend on any of the</span>
<span>previous tree calcations.</span></p>
</li>
<li>
<p><span>If we assume that the naive code implementation has a textbook 101 level tree implementation, with a pointer to a node, then we</span>&rsquo;<span>ll have a lot of pointer chasing.</span></p>
</li>
<li>
<p><span>The other thing that sticks out is the number of if statements. Each one of these is a good chance for</span>
<span>a branch mispredict, which will hurt overall latency and really impact p99 values.</span></p>
</li>
</ol>
<p><span>On my M3 max, something like that benchmarks at</span></p>
<ul>
<li>
<span>min 1.42 µs</span>
</li>
<li>
<span>p50 1.50 µs</span>
</li>
<li>
<span>p99 1.62 µs</span>
</li>
<li>
<span>max 43.46 µs</span>
</li>
</ul>
<p><span>Reasonably fast for a first pass, but not sub microsecond. And with a pretty gnarly max time.</span></p>
</section>
<section id="Flattening">

    <h2>
    <a href="#Flattening"><span>Flattening</span> </a>
    </h2>
<p><span>The pointer chasing and cache thrashing felt like low hanging fruit.</span></p>
<p><span>Sticking everything in a few arrays should help to drive down latency.</span></p>
<p><span>Using level major grouping and clever indexing, we can still walk the trees.</span></p>

<figure class="code-block">


<pre><code><span class="line">index = tree_number * (1 &lt;&lt; level) + position</span></code></pre>

</figure>
<p><span>Then we can take out the branch misprediction by massaging the go left / right calculation.</span></p>
<p><span>Instead of</span></p>

<figure class="code-block">


<pre><code><span class="line"><span class="hl-keyword">let</span> <span class="hl-variable">next_path</span> = <span class="hl-title function_ invoke__">if</span> (feature_value &gt; threshold) {</span>
<span class="line">  old_path * <span class="hl-number">2</span> + <span class="hl-number">1</span></span>
<span class="line">} <span class="hl-keyword">else</span> {</span>
<span class="line">  old_path * <span class="hl-number">2</span></span>
<span class="line">};</span></code></pre>

</figure>
<p><span>We can get that branchless by casting the bool</span></p>

<figure class="code-block">


<pre><code><span class="line"><span class="hl-keyword">let</span> <span class="hl-variable">next_path</span> = (old_path &lt;&lt; <span class="hl-number">1</span>) | ((feature_value &gt; threshold) <span class="hl-keyword">as</span> <span class="hl-type">usize</span>)</span></code></pre>

</figure>
<link rel="stylesheet" href="/assets/tree-flatten-demo.css">
<div id="tree-flatten-demo"></div>
<script type="module" src="/assets/tree-flatten-demo.js"></script>

</section>
<section id="Unrolling">

    <h2>
    <a href="#Unrolling"><span>Unrolling</span> </a>
    </h2>
<p><span>Now that we</span>&rsquo;<span>ve make cache coherency better and reduced branch mispredictions, we can work on unrolling the loop</span>
<span>and leveraging instruction level parallelism, helping to hide memory latency.</span></p>
<p><span>So instaed of something like</span></p>

<figure class="code-block">


<pre><code><span class="line">for tree in trees {</span>
<span class="line">  sum += get_leaf(tree);</span>
<span class="line">}</span></code></pre>

</figure>
<p><span>We run something like</span></p>

<figure class="code-block">


<pre><code><span class="line">let base = i * 8;</span>
<span class="line">unsafe {</span>
<span class="line">    let mut p0 = 0usize;</span>
<span class="line">    let mut p1 = 0usize;</span>
<span class="line">    // - snip</span>
<span class="line">    let mut p7 = 0usize;</span>
<span class="line"></span>
<span class="line">    for level in 0..D {</span>
<span class="line">        let lvl = level as usize;</span>
<span class="line">        let nodes_at_level = 1usize &lt;&lt; lvl;</span>
<span class="line">        let thresholds = self.level_thresholds.get_unchecked(lvl);</span>
<span class="line">        let feat_indices = self.level_features.get_unchecked(lvl);</span>
<span class="line"></span>
<span class="line">        let off0 = base * nodes_at_level + p0;</span>
<span class="line">        let off1 = (base + 1) * nodes_at_level + p1;</span>
<span class="line">        let off2 = (base + 2) * nodes_at_level + p2;</span>
<span class="line">        // - snip</span>
<span class="line">        let off7 = (base + 7) * nodes_at_level + p7;</span>
<span class="line"></span>
<span class="line">        let f0 = *features.get_unchecked(*feat_indices.get_unchecked(off0) as usize);</span>
<span class="line">        let f1 = *features.get_unchecked(*feat_indices.get_unchecked(off1) as usize);</span>
<span class="line">        // - snip </span>
<span class="line">        let f7 = *features.get_unchecked(*feat_indices.get_unchecked(off7) as usize);</span>
<span class="line"></span>
<span class="line">        let t0 = *thresholds.get_unchecked(off0);</span>
<span class="line">        let t1 = *thresholds.get_unchecked(off1);</span>
<span class="line">        // - snip</span>
<span class="line">        let t7 = *thresholds.get_unchecked(off7);</span>
<span class="line"></span>
<span class="line">        p0 = (p0 &lt;&lt; 1) | usize::from(f0 &gt; t0);</span>
<span class="line">        p1 = (p1 &lt;&lt; 1) | usize::from(f1 &gt; t1);</span>
<span class="line">        p2 = (p2 &lt;&lt; 1) | usize::from(f2 &gt; t2);</span>
<span class="line">        // - snip</span>
<span class="line">        p7 = (p7 &lt;&lt; 1) | usize::from(f7 &gt; t7);</span>
<span class="line">    }</span>
<span class="line"></span>
<span class="line">    let leaf_count = 1usize &lt;&lt; D;</span>
<span class="line">    let leaves = &amp;self.leaf_values;</span>
<span class="line">    sum += leaves.get_unchecked(base * leaf_count + p0)</span>
<span class="line">        + leaves.get_unchecked((base + 1) * leaf_count + p1)</span>
<span class="line">        + leaves.get_unchecked((base + 2) * leaf_count + p2)</span>
<span class="line">        + leaves.get_unchecked((base + 3) * leaf_count + p3)</span>
<span class="line">        + leaves.get_unchecked((base + 4) * leaf_count + p4)</span>
<span class="line">        + leaves.get_unchecked((base + 5) * leaf_count + p5)</span>
<span class="line">        + leaves.get_unchecked((base + 6) * leaf_count + p6)</span>
<span class="line">        + leaves.get_unchecked((base + 7) * leaf_count + p7);</span>
<span class="line">}</span></code></pre>

</figure>
<p><span>We can further remove some loop overhead by having the depth of the trees be a generic const.</span></p>
</section>
<section id="CPU-pinning-isolation">

    <h2>
    <a href="#CPU-pinning-isolation"><span>CPU pinning &amp; isolation</span> </a>
    </h2>
<p><span>This is my first time playing with this and it was guided heavily by Claude.</span></p>
<p><span>Spinning up a c8i server to get something with the latest gen Intel cpu that I can tweak, the following was set:</span></p>
<ul>
<li>
<span>taskset -c 2 -&gt; pin the process to CPU 2, so it doesn</span>&rsquo;<span>t get moved to a different core</span>
</li>
<li>
<span>chrt -f 99   -&gt; give the process high priority</span>
</li>
<li>
<span>isolcpus=2   -&gt; tells the scheduler to avoid placing regular tasks on our pinned core</span>
</li>
<li>
<span>nohz-full=N  -&gt; stops the ticks on this CPU? Don</span>&rsquo;<span>t understand this enough to comment, but seems to be inline with offloading housekeeping</span>
</li>
<li>
<span>rcu-nocbs=N  -&gt; Offloading hosekeeping</span>
</li>
<li>
<span>disabled the hyperthread sibling (core 98 in this case) -&gt; Intel hyperthreads share a physical core. We don</span>&rsquo;<span>t want housekeeping on the sibling thread to be using the physical core at all</span>
</li>
<li>
<span>moved interrupt requests off the pinned core</span>
</li>
<li>
<span>disabled CPU idle sleep states (C-states)</span>
</li>
<li>
<span>fixed frequency (presumably this is a power efficiency setting?) </span><code>sudo cpupower frequency-set -u 3.9GHz -d 3.9GHz</code>
</li>
</ul>
<p><span>Running these on a dedicated bare metal AWS instanced results in some </span><em><span>dramatically</span></em><span> better p99 and max values, with respsect to the p50 values. Especially when compared to my macbook running a zillion other programs.</span></p>

<figure>

<img alt="P99/P50 ratios" src="/assets/small_ratio_curves_full.png">
</figure>
</section>
<section id="Learnings">

    <h2>
    <a href="#Learnings"><span>Learnings</span> </a>
    </h2>
<p><span>This was a lot of fun. I</span>&rsquo;<span>m still wrestling with how LLMs have disrupted a lot of the puzzle solving joy I used to get from </span>
<span>programming, but this was a fun side quest to see if I could still scratch that itch in some way.</span></p>
<p><span>I should have recorded more depth in the the p99 readings. A p99.9 and p99.99 would likely have really shown how the</span>
<span>CPU isolation impacted things.</span></p>
<p><span>The m3 is fast! I</span>&rsquo;<span>d imagine SOC redudces memory latency quite a bit, which likely resulted in the smaller latencies we were</span>
<span>seeing. The inability to pin a cpu lead to some very high max values though!</span></p>
<p><span>A non-AWS instance where we could turn off SMI would have helped to mitigate some of the larger max latencies we were seeing.</span></p>
<p><span>I wonder if that also would have let us close the gap between our p99/p50 ratio and Xelera</span>&rsquo;<span>s. They had a </span><em><span>very</span></em><span> tight (10 nanos!)</span>
<span>difference between the two.</span></p>

<figure>

<img alt="Progression" src="/assets/small_progression_curves_full.png">
</figure>
</section>
]]></content>
</entry>

<entry>
<title type="text">My Favorite Test Suite</title>
<link href="https://matklad.github.io/2025/05/02/my-favorite-test-suite.html" rel="alternate" type="text/html" title="My Favorite Test Suite" />
<published>2025-05-02T00:00:00+00:00</published>
<updated>2025-05-02T00:00:00+00:00</updated>
<id>https://matklad.github.io/2025/05/02/my-favorite-test-suite</id>
<author><name>Alex Kladov</name></author>
<summary type="html"><![CDATA[tl;dr: Differential testing + determistic fuzzing = ❤️]]></summary>
<content type="html" xml:base="https://matklad.github.io/2025/05/02/my-favorite-test-suite.html"><![CDATA[
<h1><span>My Favorite Test Suite</span> <time class="meta" datetime="2025-05-02">May 2, 2025</time></h1>
<p><strong><strong><span>tl;dr: Differential testing + determistic fuzzing = ❤️</span></strong></strong></p>
<p><span>My favorite test suite, by a very large margin, comes from a project I was working</span>
<span>on a year or so ago. It was a in-house columnar database, with some clever optimizations</span>
<span>to help speed up our primary queries (OLAP-type queries across 3 different billion+ row tables joined).</span></p>
<section id="Background">

    <h2>
    <a href="#Background"><span>Background</span> </a>
    </h2>
<p><span>Columnar databases use a storage layer logically equivalent to a </span><code>Vec&lt;T&gt;</code><span> for each column.</span></p>
<p><span>Physically, these vectors split into smaller chunks, each capped at 8mb in our case.</span></p>
<p><span>Our database allowed two logical mutation operations - inserts and updates. </span>
<span>Deletes were not supported.</span></p>
<p><span>Inserts have two internal paths:</span></p>
<ul>
<li>
<span>append inserts, where the primary key is greater than the highest current primary key</span>
</li>
<li>
<span>out-of-order inserts, where the primary key is less than the highest current primary key</span>
</li>
</ul>
<p><span>Leaving us with a total of 3 possible operations:</span></p>
<ul>
<li>
<span>update</span>
</li>
<li>
<span>append</span>
</li>
<li>
<span>out-of-order insert</span>
</li>
</ul>
</section>
<section id="Operation-Examples">

    <h2>
    <a href="#Operation-Examples"><span>Operation Examples</span> </a>
    </h2>
<p><span>Consider two chunks representing a column of ints. Instead of the normal 8mb</span>
<span>cap per chunk, let</span>&rsquo;<span>s assume we have a cap of 2 items.</span></p>
<p><span>The logical represenation would be just a flat </span><code>Vec&lt;int64&gt;</code><span>, while the physical</span>
<span>represenation would be a vec of vecs (</span><code>Vec&lt;Vec&lt;int64&gt;&gt;</code><span>) showing the items in each</span>
<span>chunk.</span></p>

<figure class="code-block">


<pre><code><span class="line">Logical:  [10, 20, 30, 40]</span>
<span class="line">Physical: [10, 20][30, 40]</span></code></pre>

</figure>
<p><span>For updates, we load the relevant chunk(s), modify the value, and resave:</span></p>

<figure class="code-block">


<pre><code><span class="line">Operation: Update the 2nd item from 20 to 25</span>
<span class="line">Logical:  [10, 20, 30, 40] -&gt; [10, 25, 30, 40]</span>
<span class="line">Physical: [10, 20][30, 40] -&gt; [10, 25][30, 40]</span></code></pre>

</figure>
<p><span>Appends create new chunks when needed:</span></p>

<figure class="code-block">


<pre><code><span class="line">Operation: Append 50</span>
<span class="line">Logical:  [10, 25, 30, 40] -&gt; [10, 25, 30, 40, 50]</span>
<span class="line">Physical: [10, 25][30, 40] -&gt; [10, 25][30, 40][50]</span></code></pre>

</figure>
<p><span>Another append:</span></p>

<figure class="code-block">


<pre><code><span class="line">Operation: Append 60</span>
<span class="line">Logical:  [10, 25, 30, 40, 50] -&gt; [10, 25, 30, 40, 50, 60]</span>
<span class="line">Physical: [10, 25][30, 40][50] -&gt; [10, 25][30, 40][50, 60]</span></code></pre>

</figure>
<p><span>Out-of-order inserts require splicing and shifting values:</span></p>

<figure class="code-block">


<pre><code><span class="line">Operation: Insert 5 at position 0</span>
<span class="line">Logical:  [10, 25, 30, 40, 50] -&gt; [ 5, 10, 25, 30, 40, 50, 60]</span>
<span class="line">Physical: [10, 25][30, 40][50] -&gt; [ 5, 10][25, 30][40, 50][60]</span></code></pre>

</figure>
<p><span>Inserting at position 0 is worst-case, requiring rewriting all chunks.</span></p>
</section>
<section id="Differential-Testing">

    <h2>
    <a href="#Differential-Testing"><span>Differential Testing</span> </a>
    </h2>
<p><span>It quickly became clear that maintaining the logical representation made for an easy way to</span>
<span>test, as we could compare the results from our chunked approach, to those of a known </span>
<span>correct datastructure</span>&rsquo;<span>s like the humble </span><code>Vec</code><span>.</span></p>
<p><span>If we had a </span><code>Vec&lt;Op&gt;</code><span>, then we should be able to iterate through our chunked representation</span>
<span>and a </span><code>Vec</code><span>, comparing the results after each step to ensure that they are the same.</span></p>

<figure class="code-block">


<pre><code><span class="line"><span class="hl-keyword">pub</span> <span class="hl-keyword">enum</span> <span class="hl-title class_">Op</span>&lt;T&gt; {</span>
<span class="line">    <span class="hl-comment">/// The new values</span></span>
<span class="line">    <span class="hl-title function_ invoke__">Append</span>(<span class="hl-type">Vec</span>&lt;T&gt;),</span>
<span class="line">    <span class="hl-comment">/// The indexes and new values</span></span>
<span class="line">    <span class="hl-title function_ invoke__">Splice</span>(<span class="hl-type">Vec</span>&lt;(<span class="hl-type">usize</span>, T)&gt;),</span>
<span class="line">    <span class="hl-comment">/// The indexes and updated values</span></span>
<span class="line">    <span class="hl-title function_ invoke__">Update</span>(<span class="hl-type">Vec</span>&lt;(<span class="hl-type">usize</span>, T)&gt;),</span>
<span class="line">}</span>
<span class="line"></span>
<span class="line"><span class="hl-keyword">let</span> <span class="hl-variable">ops</span>: <span class="hl-type">Vec</span>&lt;Op&lt;<span class="hl-type">i64</span>&gt;&gt; = <span class="hl-title function_ invoke__">generate_random_ops</span>();</span>
<span class="line"></span>
<span class="line"><span class="hl-keyword">let</span> <span class="hl-keyword">mut </span><span class="hl-variable">expected</span> = <span class="hl-type">Vec</span>::&lt;<span class="hl-type">i64</span>&gt;::<span class="hl-title function_ invoke__">new</span>();</span>
<span class="line"><span class="hl-keyword">let</span> <span class="hl-keyword">mut </span><span class="hl-variable">actual</span> = ChunkedColumn::&lt;<span class="hl-type">i64</span>&gt;::<span class="hl-title function_ invoke__">new</span>();</span>
<span class="line"></span>
<span class="line"><span class="hl-keyword">for</span> <span class="hl-variable">op</span> <span class="hl-keyword">in</span> ops {</span>
<span class="line">    expected.<span class="hl-title function_ invoke__">apply</span>(op);</span>
<span class="line">    actual.<span class="hl-title function_ invoke__">apply</span>(op);</span>
<span class="line"></span>
<span class="line">    <span class="hl-built_in">assert_eq!</span>(expected, actual.<span class="hl-title function_ invoke__">to_vec</span>());</span>
<span class="line">}</span></code></pre>

</figure>
</section>
<section id="Determistic-fuzzing">

    <h2>
    <a href="#Determistic-fuzzing"><span>Determistic fuzzing</span> </a>
    </h2>
<p><span>Fuzzing allowed us to test a </span><em><span>lot</span></em><span> more edge cases than the unit tests we</span>
<span>had written prior, which we thought were extensive!</span></p>
<p><span>By using a given seed, it allowed us t oeasily reproduce any failing test</span>
<span>cases as new unit tests.</span></p>

<figure class="code-block">


<pre><code><span class="line"><span class="hl-keyword">const</span> ITERATIONS: <span class="hl-type">usize</span> = <span class="hl-number">100_000</span>;</span>
<span class="line"></span>
<span class="line"><span class="hl-keyword">fn</span> <span class="hl-title function_">check</span>(seed: <span class="hl-type">u64</span>) {</span>
<span class="line">    <span class="hl-comment">// Similar to above, but we pass a PRNG in to our random op generator</span></span>
<span class="line">    <span class="hl-keyword">let</span> <span class="hl-keyword">mut </span><span class="hl-variable">rng</span> = StdRng::<span class="hl-title function_ invoke__">seed_from_u64</span>(seed);</span>
<span class="line">    <span class="hl-keyword">let</span> <span class="hl-variable">ops</span>: <span class="hl-type">Vec</span>&lt;Op&lt;<span class="hl-type">i64</span>&gt;&gt; = <span class="hl-title function_ invoke__">generate_random_ops</span>(&amp;<span class="hl-keyword">mut</span> rng);</span>
<span class="line"></span>
<span class="line">    <span class="hl-keyword">let</span> <span class="hl-keyword">mut </span><span class="hl-variable">expected</span> = <span class="hl-type">Vec</span>::&lt;<span class="hl-type">i64</span>&gt;::<span class="hl-title function_ invoke__">new</span>();</span>
<span class="line">    <span class="hl-keyword">let</span> <span class="hl-keyword">mut </span><span class="hl-variable">actual</span> = ChunkedColumn::&lt;<span class="hl-type">i64</span>&gt;::<span class="hl-title function_ invoke__">new</span>();</span>
<span class="line"></span>
<span class="line">    <span class="hl-keyword">for</span> <span class="hl-variable">op</span> <span class="hl-keyword">in</span> ops {</span>
<span class="line">        expected.<span class="hl-title function_ invoke__">apply</span>(op);</span>
<span class="line">        actual.<span class="hl-title function_ invoke__">apply</span>(op);</span>
<span class="line"></span>
<span class="line">        <span class="hl-comment">// And if something fails, we print the seed, allowing us to</span></span>
<span class="line">        <span class="hl-comment">// quickly and easily reproduce the failing pattern</span></span>
<span class="line">        <span class="hl-built_in">assert_eq!</span>(expected, actual.<span class="hl-title function_ invoke__">to_vec</span>(), <span class="hl-string">&quot;[seed:{seed}] Mismatch detected: {:#?} vs {:?}&quot;</span>, expected, actual);</span>
<span class="line">    }</span>
<span class="line">}</span>
<span class="line"></span>
<span class="line"><span class="hl-keyword">for</span> <span class="hl-variable">_</span> <span class="hl-keyword">in</span> <span class="hl-number">0</span>..ITERATIONS {</span>
<span class="line">    <span class="hl-comment">// Generate a completely random seed for each iteration</span></span>
<span class="line">    <span class="hl-keyword">let</span> <span class="hl-variable">seed</span>: <span class="hl-type">u64</span> = rand::<span class="hl-title function_ invoke__">random</span>();</span>
<span class="line"></span>
<span class="line">    <span class="hl-comment">// Then use that to generate the random ops</span></span>
<span class="line">    <span class="hl-title function_ invoke__">check</span>(seed);</span>
<span class="line">}</span></code></pre>

</figure>
</section>
<section id="In-closing">

    <h2>
    <a href="#In-closing"><span>In closing</span> </a>
    </h2>
<p><span>The combination of differential testing and determistic fuzzing gave us an</span>
<span>incredibly high degree of confidence that things were working as they should be.</span></p>
<p><span>In the first few runs of the fuzzer, when we were still finding new bugs, it gave</span>
<span>us a wonderfully simple way to reproduce those failing test cases as new unit tests.</span></p>
</section>
]]></content>
</entry>

<entry>
<title type="text">List Shrinking</title>
<link href="https://matklad.github.io/2024/07/26/list-shrinking.html" rel="alternate" type="text/html" title="List Shrinking" />
<published>2024-07-26T00:00:00+00:00</published>
<updated>2024-07-26T00:00:00+00:00</updated>
<id>https://matklad.github.io/2024/07/26/list-shrinking</id>
<author><name>Alex Kladov</name></author>
<summary type="html"><![CDATA[Code Link]]></summary>
<content type="html" xml:base="https://matklad.github.io/2024/07/26/list-shrinking.html"><![CDATA[
<h1><span>List Shrinking</span> <time class="meta" datetime="2024-07-26">Jul 26, 2024</time></h1>
<p><a href="https://github.com/tmyers273/shrink"><span>Code Link</span></a></p>
<p><span>I</span>&rsquo;<span>ve been working on a conversion process, where we take a large struct </span><code>A</code><span> and, depending on</span>
<span>various values within it, convert it to either </span><code>B</code><span>, </span><code>C</code><span>, or </span><code>D</code><span>.</span></p>
<p><span>This data comes from an external source and the documentation leaves a lot to be desired, with</span>
<span>missing enum variants, incorrect type fields, incorrect nullability, etc.</span></p>
<p><span>Due to the inaccurate documentation, we lean heavily on our test suite.</span></p>
<p><span>There are a large number of items (hundreds of millions) that we deal with in this conversion</span>
<span>process. While we certainly could pull all those items and run them in our tests, I thought</span>
<span>there might be a better way. One that would minimize the pain of running the tests while</span>
<span>preserving the same level of confidence.</span></p>
<p><span>What if we could throw away all the items that are not uniquely interesting? Or are, if you</span>
<span>squint your eyes, near-duplicates of other items?</span></p>
<p><span>More concretely, imagine the following struct</span></p>

<figure class="code-block">


<pre><code><span class="line"><span class="hl-keyword">struct</span> <span class="hl-title class_">MyStruct</span> {</span>
<span class="line">    a: <span class="hl-type">u8</span>,</span>
<span class="line">    b: <span class="hl-type">String</span>,</span>
<span class="line">}</span></code></pre>

</figure>
<p><span>These two records are, of course, not equal.</span></p>

<figure class="code-block">


<pre><code><span class="line"><span class="hl-keyword">let</span> <span class="hl-variable">a</span> = MyStruct { a: <span class="hl-number">1</span>, b: <span class="hl-string">&quot;hello&quot;</span>.<span class="hl-title function_ invoke__">to_string</span>() };</span>
<span class="line"><span class="hl-keyword">let</span> <span class="hl-variable">b</span> = MyStruct { a: <span class="hl-number">2</span>, b: <span class="hl-string">&quot;hello world&quot;</span>.<span class="hl-title function_ invoke__">to_string</span>() };</span></code></pre>

</figure>
<p><span>But, in the conversion process, we wanted to treat them as equal. They both contain a positive,</span>
<span>non-max integer and a non-empty, non-whitespace string. If one of them converts successfully,</span>
<span>then the other will as well.</span></p>
<p><span>What I really was after was a way extract just the meaningfully unique items. So what exactly</span>
<span>is a meaningfully unique item?</span></p>

<figure class="code-block">


<pre><code><span class="line"><span class="hl-comment">// A positive int is one type</span></span>
<span class="line"><span class="hl-keyword">let</span> <span class="hl-variable">a</span> = MyStruct { a: <span class="hl-number">1</span>, b: <span class="hl-string">&quot;hello&quot;</span>.<span class="hl-title function_ invoke__">to_string</span>() };</span>
<span class="line"></span>
<span class="line"><span class="hl-comment">// 0 is another interesting one</span></span>
<span class="line"><span class="hl-keyword">let</span> <span class="hl-variable">b</span> = MyStruct { a: <span class="hl-number">0</span>, b: <span class="hl-string">&quot;hello world&quot;</span>.<span class="hl-title function_ invoke__">to_string</span>() };</span>
<span class="line"></span>
<span class="line"><span class="hl-comment">// Along with the max value</span></span>
<span class="line"><span class="hl-keyword">let</span> <span class="hl-variable">c</span> = MyStruct { a: <span class="hl-type">u8</span>::MAX, b: <span class="hl-string">&quot;hello world&quot;</span>.<span class="hl-title function_ invoke__">to_string</span>() };</span>
<span class="line"></span>
<span class="line"><span class="hl-comment">// Or an empty string</span></span>
<span class="line"><span class="hl-keyword">let</span> <span class="hl-variable">c</span> = MyStruct { a: <span class="hl-number">1</span>, b: <span class="hl-string">&quot;&quot;</span>.<span class="hl-title function_ invoke__">to_string</span>() };</span>
<span class="line"></span>
<span class="line"><span class="hl-comment">// Or a string of just whitespace</span></span>
<span class="line"><span class="hl-keyword">let</span> <span class="hl-variable">c</span> = MyStruct { a: <span class="hl-number">1</span>, b: <span class="hl-string">&quot;   &quot;</span>.<span class="hl-title function_ invoke__">to_string</span>() };</span></code></pre>

</figure>
<p><span>That allows us to start to pencil in some basic rules and some enums to represent them.</span></p>
<section id="Strings">

    <h2>
    <a href="#Strings"><span>Strings</span> </a>
    </h2>
<p><span>Perhaps the simplest case. There are only a few interesting strings that we care about:</span></p>

<figure class="code-block">


<pre><code><span class="line"><span class="hl-keyword">enum</span> <span class="hl-title class_">StringClassification</span> {</span>
<span class="line">    Empty,</span>
<span class="line">    Whitespace,</span>
<span class="line">    NonEmpty,</span>
<span class="line">}</span></code></pre>

</figure>
</section>
<section id="Integers">

    <h2>
    <a href="#Integers"><span>Integers</span> </a>
    </h2>
<p><span>Integers have a few more options.</span></p>

<figure class="code-block">


<pre><code><span class="line"><span class="hl-keyword">enum</span> <span class="hl-title class_">SignedIntClassification</span> {</span>
<span class="line">    Min,</span>
<span class="line">    Negative,</span>
<span class="line">    Zero,</span>
<span class="line">    Positive,</span>
<span class="line">    Max,</span>
<span class="line">}</span>
<span class="line"></span>
<span class="line"><span class="hl-keyword">enum</span> <span class="hl-title class_">UnsignedIntClassification</span> {</span>
<span class="line">    Zero,</span>
<span class="line">    Positive,</span>
<span class="line">    Max,</span>
<span class="line">}</span></code></pre>

</figure>
</section>
<section id="Tying-it-together">

    <h2>
    <a href="#Tying-it-together"><span>Tying it together</span> </a>
    </h2>
<p><span>Let</span>&rsquo;<span>s introduce a trait.</span></p>

<figure class="code-block">


<pre><code><span class="line"><span class="hl-keyword">trait</span> <span class="hl-title class_">Classify</span> {</span>
<span class="line">    <span class="hl-keyword">type</span> <span class="hl-title class_">Output</span>: Hash;</span>
<span class="line"></span>
<span class="line">    <span class="hl-keyword">fn</span> <span class="hl-title function_">classify</span>(&amp;<span class="hl-keyword">self</span>) <span class="hl-punctuation">-&gt;</span> <span class="hl-keyword">Self</span>::Output;</span>
<span class="line">}</span>
<span class="line"></span>
<span class="line"><span class="hl-keyword">impl</span> <span class="hl-title class_">Classify</span> <span class="hl-keyword">for</span> <span class="hl-title class_">u8</span> {</span>
<span class="line">    <span class="hl-keyword">type</span> <span class="hl-title class_">Output</span> = UnsignedIntClassification;</span>
<span class="line"></span>
<span class="line">    <span class="hl-keyword">fn</span> <span class="hl-title function_">classify</span>(&amp;<span class="hl-keyword">self</span>) <span class="hl-punctuation">-&gt;</span> <span class="hl-keyword">Self</span>::Output {</span>
<span class="line">        <span class="hl-keyword">match</span> <span class="hl-keyword">self</span> {</span>
<span class="line">            <span class="hl-number">0</span> =&gt; UnsignedIntClassification::Zero,</span>
<span class="line">            <span class="hl-type">u8</span>::MAX =&gt; UnsignedIntClassification::Max,</span>
<span class="line">            _ =&gt; UnsignedIntClassification::Positive,</span>
<span class="line">        }</span>
<span class="line">    }</span>
<span class="line">}</span>
<span class="line"></span>
<span class="line"><span class="hl-keyword">impl</span> <span class="hl-title class_">Classify</span> <span class="hl-keyword">for</span> <span class="hl-title class_">String</span> {</span>
<span class="line">    <span class="hl-keyword">type</span> <span class="hl-title class_">Output</span> = StringClassification;</span>
<span class="line"></span>
<span class="line">    <span class="hl-keyword">fn</span> <span class="hl-title function_">classify</span>(&amp;<span class="hl-keyword">self</span>) <span class="hl-punctuation">-&gt;</span> <span class="hl-keyword">Self</span>::Output {</span>
<span class="line">        <span class="hl-keyword">match</span> <span class="hl-keyword">self</span>.<span class="hl-title function_ invoke__">as_str</span>() {</span>
<span class="line">            <span class="hl-string">&quot;&quot;</span> =&gt; StringClassification::Empty,</span>
<span class="line">            s <span class="hl-keyword">if</span> s.<span class="hl-title function_ invoke__">chars</span>().<span class="hl-title function_ invoke__">all</span>(<span class="hl-type">char</span>::is_whitespace) =&gt; StringClassification::Whitespace,</span>
<span class="line">            _ =&gt; StringClassification::NonEmpty,</span>
<span class="line">        }</span>
<span class="line">    }</span>
<span class="line">}</span></code></pre>

</figure>
<p><span>That let</span>&rsquo;<span>s us classify some primitives, which is a start. But how do we classify the structs</span>
<span>we introduced earlier?</span></p>

<figure class="code-block">


<pre><code><span class="line"><span class="hl-keyword">let</span> <span class="hl-variable">a</span> = MyStruct { a: <span class="hl-number">1</span>, b: <span class="hl-string">&quot;hello&quot;</span>.<span class="hl-title function_ invoke__">to_string</span>() };</span>
<span class="line"><span class="hl-keyword">let</span> <span class="hl-variable">b</span> = MyStruct { a: <span class="hl-number">2</span>, b: <span class="hl-string">&quot;hello world&quot;</span>.<span class="hl-title function_ invoke__">to_string</span>() };</span></code></pre>

</figure>
<p><span>While we certainly could return a tuple of</span></p>
<p><code>(UnsignedIntClassification, StringClassification)</code></p>
<p><span>that approach doesn</span>&rsquo;<span>t scale well. The large struct </span><code>A</code><span> has 20 some fields and, more importantly,</span>
<span>has nested fields, for a total of 60+ fields.</span></p>
<p><span>If we make sure that our base enum classification types implement </span><code>Hash</code><span>, we can use that to</span>
<span>hash the classifications of the fields, giving us some really nice compositional properties.</span></p>

<figure class="code-block">


<pre><code><span class="line"><span class="hl-keyword">impl</span> <span class="hl-title class_">Classify</span> <span class="hl-keyword">for</span> <span class="hl-title class_">MyStruct</span> {</span>
<span class="line">    <span class="hl-keyword">type</span> <span class="hl-title class_">Output</span> = <span class="hl-type">u64</span>;</span>
<span class="line"></span>
<span class="line">    <span class="hl-keyword">fn</span> <span class="hl-title function_">classify</span>(&amp;<span class="hl-keyword">self</span>) <span class="hl-punctuation">-&gt;</span> <span class="hl-keyword">Self</span>::Output {</span>
<span class="line">        <span class="hl-keyword">let</span> <span class="hl-keyword">mut </span><span class="hl-variable">hasher</span> = DefaultHasher::<span class="hl-title function_ invoke__">new</span>();</span>
<span class="line">        &amp;<span class="hl-keyword">self</span>.a.<span class="hl-title function_ invoke__">classify</span>().<span class="hl-title function_ invoke__">hash</span>(&amp;<span class="hl-keyword">mut</span> hasher);</span>
<span class="line">        &amp;<span class="hl-keyword">self</span>.b.<span class="hl-title function_ invoke__">classify</span>().<span class="hl-title function_ invoke__">hash</span>(&amp;<span class="hl-keyword">mut</span> hasher);</span>
<span class="line">        hasher.<span class="hl-title function_ invoke__">finish</span>()</span>
<span class="line">    }</span>
<span class="line">}</span></code></pre>

</figure>
</section>
<section id="More-complex-structs">

    <h2>
    <a href="#More-complex-structs"><span>More complex structs</span> </a>
    </h2>
<p><span>That gets things working for our simple example, but what about something with some more</span>
<span>complexity? Perhaps a struct like</span></p>

<figure class="code-block">


<pre><code><span class="line"><span class="hl-keyword">struct</span> <span class="hl-title class_">ComplexStruct</span> {</span>
<span class="line">    a: <span class="hl-type">Option</span>&lt;<span class="hl-type">u8</span>&gt;,</span>
<span class="line">    b: <span class="hl-type">Vec</span>&lt;<span class="hl-type">String</span>&gt;,</span>
<span class="line">}</span></code></pre>

</figure>
<section id="Options">

    <h3>
    <a href="#Options"><span>Options</span> </a>
    </h3>
<p><span>Options can be thought of as just adding an additional </span><code>None</code><span> variant to the classification.</span></p>
</section>
<section id="Vecs-arrays-and-slices">

    <h3>
    <a href="#Vecs-arrays-and-slices"><span>Vecs, arrays, and slices</span> </a>
    </h3>
<p><span>This is where things get a bit more interesting. We want a classification that is order-independent</span>
<span>and doesn</span>&rsquo;<span>t care about duplicates.</span></p>
<p><span>We can accomplish that be classifying each element. Then, removing any duplicates and sorting the</span>
<span>remaining elements.</span></p>
<p><span>This allows something like the following to be true:</span></p>

<figure class="code-block">


<pre><code><span class="line"><span class="hl-keyword">let</span> <span class="hl-variable">a</span> = <span class="hl-built_in">vec!</span>[<span class="hl-number">1u8</span>, <span class="hl-number">0</span>];</span>
<span class="line"><span class="hl-keyword">let</span> <span class="hl-variable">b</span> = <span class="hl-built_in">vec!</span>[<span class="hl-number">0u8</span>, <span class="hl-number">5</span>, <span class="hl-number">6</span>];</span>
<span class="line"><span class="hl-built_in">assert_eq!</span>(a.<span class="hl-title function_ invoke__">classify</span>(), b.<span class="hl-title function_ invoke__">classify</span>());</span></code></pre>

</figure>
<p><span>A BTreeSet does the heavy lifting for us here.</span></p>

<figure class="code-block">


<pre><code><span class="line"><span class="hl-keyword">impl</span>&lt;T: Classify&gt; Classify <span class="hl-keyword">for</span> <span class="hl-title class_">Vec</span>&lt;T&gt;</span>
<span class="line"><span class="hl-keyword">where</span></span>
<span class="line">    T::Output: Hash + <span class="hl-built_in">Eq</span> + <span class="hl-built_in">Ord</span>,</span>
<span class="line">{</span>
<span class="line">    <span class="hl-keyword">type</span> <span class="hl-title class_">Output</span> = <span class="hl-type">u64</span>;</span>
<span class="line"></span>
<span class="line">    <span class="hl-keyword">fn</span> <span class="hl-title function_">classify</span>(&amp;<span class="hl-keyword">self</span>) <span class="hl-punctuation">-&gt;</span> <span class="hl-keyword">Self</span>::Output {</span>
<span class="line">        <span class="hl-keyword">let</span> <span class="hl-variable">unique_classifications</span> = BTreeSet::<span class="hl-title function_ invoke__">default</span>();</span>
<span class="line">        <span class="hl-keyword">for</span> <span class="hl-variable">item</span> <span class="hl-keyword">in</span> &amp;<span class="hl-keyword">self</span> {</span>
<span class="line">            unique_classifications.<span class="hl-title function_ invoke__">insert</span>(item.<span class="hl-title function_ invoke__">classify</span>());</span>
<span class="line">        }</span>
<span class="line"></span>
<span class="line">        <span class="hl-keyword">let</span> <span class="hl-keyword">mut </span><span class="hl-variable">hasher</span> = DefaultHasher::<span class="hl-title function_ invoke__">new</span>();</span>
<span class="line">        <span class="hl-keyword">for</span> <span class="hl-variable">classification</span> <span class="hl-keyword">in</span> unique_classifications {</span>
<span class="line">            classification.<span class="hl-title function_ invoke__">hash</span>(&amp;<span class="hl-keyword">mut</span> hasher);</span>
<span class="line">        }</span>
<span class="line">        hasher.<span class="hl-title function_ invoke__">finish</span>()</span>
<span class="line">    }</span>
<span class="line">}</span></code></pre>

</figure>
<p><span>But, it does fail to capture an important property of the vec. We really would like to capture</span>
<span>whether the vec:</span></p>
<ul>
<li>
<span>is empty,</span>
</li>
<li>
<span>contains a single element, or</span>
</li>
<li>
<span>contains multiple elements</span>
</li>
</ul>
<p><span>That remains an item for a different day.</span></p>
</section>
</section>
<section id="Results">

    <h2>
    <a href="#Results"><span>Results</span> </a>
    </h2>
<p><span>We have a few different datasets in which we employ this pattern and the results have been</span>
<span>very successful. We are able to keep a gzipped version of the dataset in our repository to</span>
<span>use in our tests.</span></p>
<table>
<tr>
<th><span>Dataset</span></th>
<th style="text-align: right;"><span>Meaningfully Unique Items</span></th>
<th style="text-align: right;"><span>Total Items</span></th>
</tr>
<tr>
<td><span>First</span></td>
<td style="text-align: right;"><span>620</span></td>
<td style="text-align: right;"><span>40,000</span></td>
</tr>
<tr>
<td><span>Second</span></td>
<td style="text-align: right;"><span>172</span></td>
<td style="text-align: right;"><span>50,000</span></td>
</tr>
<tr>
<td><span>Third</span></td>
<td style="text-align: right;"><span>681</span></td>
<td style="text-align: right;"><span>700,000</span></td>
</tr>
<tr>
<td><span>Fourth</span></td>
<td style="text-align: right;"><span>3,876</span></td>
<td style="text-align: right;"><span>175,000,000</span></td>
</tr>
</table>
</section>
]]></content>
</entry>

</feed>
